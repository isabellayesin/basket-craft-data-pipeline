version: 2

models:
  - name: stg_orders
    description: Staging model for orders
    columns:
      - name: order_id
        tests:
          - unique
          - not_null
      - name: customer_id
      - name: product_id
      - name: order_date
      - name: quantity
      - name: loaded_at
